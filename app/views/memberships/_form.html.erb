<%= form_for(@membership, remote: true) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :group  %><br>
    <% options = options_from_collection_for_select(@groups, 'id', 'name') %>
    <%= f.select :group_id,  options %>
  </div>
  <div class="field">
    <%= f.label :user  %><br>
    <% options = options_from_collection_for_select(@users, 'id', 'user_name') %>
    <%= f.select :user_id,  options %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
